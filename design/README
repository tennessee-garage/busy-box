Files related to the design of the busy box.  This include illustrator files of the individual controls as well as different layouts using them in different applications.

The original application was a childs busy box (e.g. lots of buttons & dials that give engaging feedback, but functionally does nothing).
